<div {{ attributes.defaults({ class: 'w-full flex flex-col aspect-video justify-center items-center  bg-white dark:bg-clear text-black border border-black/10 p-8 border-2 rounded-lg border-dark dark:border-clear' }) }}>
    {% block content %} {% endblock %}
</div>
