<div {{ attributes.defaults({ class: 'w-full h-full flex flex-col justify-center items-center space-y-8 bg-white dark:bg-clear text-black border border-black/10 px-6 py-8 border-4 rounded-xl border-dark dark:border-clear' }) }}>
<h2 class="font-black">
    {% block content %} {% endblock %}
    </h2>
</div>
