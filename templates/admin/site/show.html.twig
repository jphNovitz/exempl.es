{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}
    <h1>Site</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ site.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ site.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ site.image }}</td>
            </tr>
            <tr>
                <th>Repo</th>
                <td>{{ site.repo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_site_index') }}">back to list</a>

    <a href="{{ path('app_admin_site_edit', {'id': site.id}) }}">edit</a>

    {{ include('admin/site/_delete_form.html.twig') }}
{% endblock %}
